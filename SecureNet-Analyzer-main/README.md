# SecureNet Analyzer

The canonical project documentation is maintained in the repository root:

**[Open the SecureNet Analyzer README](../README.md)**

The application source code is in this directory:

`Main.py` + `Utils/`

Run from this directory:

```powershell
python Main.py --help
python test_all.py
```
